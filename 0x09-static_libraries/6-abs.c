#include "main.h"

/**
 * _abs - A function that prints the absolute value of any number
 * @n: integer to be transformed
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
